import { Redirect } from 'expo-router';

// The admin _layout.jsx handles all rendering.
// This index just ensures expo-router can resolve the route.
export default function AdminIndex() {
  return null;
}
